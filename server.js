var express = require("express");

var PORT = process.env.PORT || 8080;
var app = express();

//Serve static content for the app from the "public" director

//Parse application body as JSON

//Require Handlebars

//Sett Handlebars engine and define "main"